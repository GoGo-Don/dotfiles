- date: 2026-04-21
  files: /etc/acpi/{tlp-toggle, handler}
  description: Fn key combo bound to tlp-cycle script for cycling TLP power profiles
  tags: [acpi, power, tlp]
  revert: remove /etc/acpi/events/tlp-cycle, restart acpid

- date: YYYY-MM-DD
  file: /path/to/changed/file
  description: what changed and why
  tags: [tag1, tag2]
  revert: how to undo
